<UserControl x:Class="NotationTB.UserControl.ProductStandartSettingUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NotationTB.UserControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <StackPanel Orientation="Vertical" Margin="5">
            <StackPanel Orientation="Horizontal">
                <ComboBox Width="150" />
                <Button Content="Добавить стандарт" />
            </StackPanel>

            <!-- Операции -->
            <ScrollViewer Height="60" VerticalScrollBarVisibility="Auto">
                <WrapPanel x:Name="OperationCheckBoxPanel" Margin="0,5,0,0"/>
            </ScrollViewer>
        </StackPanel>

        <!-- Таблица -->
        <DataGrid x:Name="OperationsGrid" Grid.Row="1" AutoGenerateColumns="False" Margin="5"/>
    </Grid>
</UserControl>